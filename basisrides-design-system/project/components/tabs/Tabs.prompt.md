Underline tab bar. Uncontrolled by default (tracks its own active tab); pass `activeId`+`onChange` to control it.

```jsx
<Tabs tabs={[{id:'upcoming',label:'Upcoming'},{id:'past',label:'Past'}]} />
```
