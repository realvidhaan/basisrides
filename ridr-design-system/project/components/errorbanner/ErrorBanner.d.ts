/**
 * ErrorBanner — inline warm error message. Renders nothing without a message.
 * @startingPoint section="Components" subtitle="Inline error banner" viewport="700x90"
 */
export interface ErrorBannerProps {
  message?: string;
}
