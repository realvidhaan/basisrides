Primary action button. Use for form submits and main CTAs.

```jsx
<Button label="Log in" variant="primary" onClick={handleLogin} />
```

Variants: primary (filled teal), outline (dark ring), ghost (text-only). Sizes: sm/md/lg. `loading` swaps the label for "Loading…".
