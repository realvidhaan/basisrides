/**
 * Card — flat bordered container. No drop shadows, matching the flat Ridr surface style.
 * @startingPoint section="Components" subtitle="Flat bordered container" viewport="700x160"
 */
export interface CardProps {
  children?: any;
  padding?: number;
  bordered?: boolean;
}
